global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    project: baba-stock-prediction

scrape_configs:
  - job_name: baba-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000
